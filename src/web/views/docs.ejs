<!DOCTYPE html>
<html lang="zh-TW">

<head>
    <meta charset="UTF-8">
    <title>使用文檔 - VX6</title>
    <link rel="icon" href="/favicon.jpg" type="image/jpeg">
    <link rel="stylesheet" href="/style.css">
    <link
        href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;600;800&family=Inter:wght@400;600&family=Noto+Sans+TC:wght@400;500;700&display=swap"
        rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
</head>

<body>
    <%- include('partials/sidebar', { path: '/docs' }) %>

        <div class="main-content">
            <div class="topbar">
                <h2>使用文檔 (Documentation)</h2>
                <div class="user-menu" style="background: var(--bg-card); border: var(--glass-border);">
                    <i class="fa-solid fa-search" style="color: var(--text-secondary);"></i>
                    <input type="text" id="searchInput" placeholder="搜尋文檔..."
                        style="background: transparent; border: none; outline: none; color: var(--text-primary); padding-left: 0.5rem; width: 200px;">
                </div>
            </div>

            <div class="dashboard-grid" style="grid-template-columns: 1fr;">

                <div class="card doc-section">
                    <h3><i class="fa-solid fa-rocket" style="color: var(--accent-color);"></i> 快速開始</h3>
                    <p>歡迎使用 VX6 機器人！這是一個專為社群管理設計的高級 Discord 機器人。請按照以下步驟開始使用：</p>
                    <ol style="color: var(--text-secondary); line-height: 1.8; margin-left: 1.5rem;">
                        <li>點擊左側的「邀請機器人」將 Bot 加入您的伺服器。</li>
                        <li>給予機器人 <code>Administrator</code> 或 <code>Manage Server</code> 權限以確保功能正常。</li>
                        <li>前往「伺服器列表」，點擊您伺服器的「管理伺服器」按鈕。</li>
                    </ol>
                </div>

                <div class="card doc-section">
                    <h3><i class="fa-solid fa-bullhorn" style="color: var(--warning-color);"></i> 發送公告</h3>
                    <p>您可以在網頁控制台直接發送精美的 Embed 公告：</p>
                    <ul style="color: var(--text-secondary); line-height: 1.8; margin-left: 1.5rem;">
                        <li>進入伺服器設定頁面。</li>
                        <li>找到「發送公告」區塊。</li>
                        <li>填寫標題、內容 (支援 Markdown) 並選擇頻道。</li>
                        <li>點擊發送，機器人即會同步發布。</li>
                    </ul>
                </div>

                <div class="card doc-section">
                    <h3><i class="fa-solid fa-wand-magic-sparkles" style="color: #FF5252;"></i> 自訂指令 (Custom Commands)
                    </h3>
                    <p>管理專屬於您伺服器的指令：</p>
                    <ul style="color: var(--text-secondary); line-height: 1.8; margin-left: 1.5rem;">
                        <li>前往 Dashboard > 自訂指令管理。</li>
                        <li>輸入「觸發詞」(Bot 偵測到此訊息) 與「回應內容」。</li>
                        <li>範例：觸發 <code>!rules</code> -> 回應 <code>請遵守伺服器規範...</code></li>
                    </ul>
                </div>

                <div class="card doc-section">
                    <h3><i class="fa-solid fa-shield-halved" style="color: #69F0AE;"></i> 自動管理 (Auto-Mod)</h3>
                    <p>保護您的伺服器免受惡意內容侵害：</p>
                    <ul style="color: var(--text-secondary); line-height: 1.8; margin-left: 1.5rem;">
                        <li><strong>阻擋連結</strong>：開啟後，將自動刪除包含 http/https 的訊息。</li>
                        <li><strong>關鍵字過濾</strong>：設定禁字，Bot 將自動刪除包含這些詞彙的訊息。</li>
                    </ul>
                </div>

                <div class="card doc-section">
                    <h3><i class="fa-solid fa-chart-line" style="color: #F47FFF;"></i> 等級系統 (Leveling)</h3>
                    <p>增加社群活耀度：</p>
                    <ul style="color: var(--text-secondary); line-height: 1.8; margin-left: 1.5rem;">
                        <li>成員每分鐘發言可獲得隨機 XP。</li>
                        <li><strong>排行榜</strong>：Dashboard 提供公開的 Top 50 等級排行榜。</li>
                        <li><strong>升級通知</strong>：當成員升級時，Bot 會在頻道發送賀詞。</li>
                    </ul>
                </div>

                <div class="card doc-section">
                    <h3><i class="fa-solid fa-ticket" style="color: #2ECC71;"></i> 客服單系 (Ticket System)</h3>
                    <p>專業的客服支援流程：</p>
                    <ul style="color: var(--text-secondary); line-height: 1.8; margin-left: 1.5rem;">
                        <li><strong>自訂類別</strong>：在 Dashboard 設定不同的客服類型 (如：技術支援、舉報)。</li>
                        <li><strong>發送面板</strong>：使用 <code>/panel</code> 指令發送客服選單。</li>
                        <li><strong>歷史紀錄</strong>：客服單關閉後，對話紀錄會自動保存，可於 Dashboard 隨時查看。</li>
                    </ul>
                </div>

                <div class="card doc-section">
                    <h3><i class="fa-solid fa-music" style="color: #F39C12;"></i> 音樂播放器 (Music)</h3>
                    <p>高品質的音樂體驗與網頁控制：</p>
                    <ul style="color: var(--text-secondary); line-height: 1.8; margin-left: 1.5rem;">
                        <li><strong>指令</strong>：<code>/music play [歌名]</code>, <code>/music skip</code>,
                            <code>/music stop</code>。
                        </li>
                        <li><strong>網頁控制</strong>：在 Dashboard 的音樂播放器頁面，您可以查看目前播放歌曲、調整進度與佇列。</li>
                    </ul>
                </div>

                <div class="card doc-section" id="github-section">
                    <h3><i class="fa-brands fa-github" style="color: var(--text-primary);"></i> 開源專案</h3>
                    <p>本專案已開源於 GitHub，歡迎提交 Issue 或 Pull Request。</p>
                    <a href="https://github.com/huchialun9-ctrl/-dc-.git" target="_blank" class="btn-submit"
                        style="display: inline-block; text-decoration: none; margin-top: 1rem;">
                        前往 GitHub Repository <i class="fa-solid fa-external-link-alt"></i>
                    </a>
                </div>

            </div>
        </div>

        <script>
            // Simple Client-side Search
            document.getElementById('searchInput').addEventListener('keyup', function (e) {
                const term = e.target.value.toLowerCase();
                const sections = document.querySelectorAll('.doc-section');

                sections.forEach(section => {
                    const text = section.innerText.toLowerCase();
                    if (text.includes(term)) {
                        section.style.display = 'block';
                    } else {
                        section.style.display = 'none';
                    }
                });
            });
        </script>
</body>

</html>